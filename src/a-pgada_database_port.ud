(section
  (ref "pgada_database_port")
  (title "PGAda.Database.Port")
  (para function_desc "Return database port.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis
"with PGAda.Database;

function Port (Connection : in PGAda.Database.Connection_T) return Positive;
"))
  (subsection
    (title "Description")
    (para
      "The " (item ada_function "Port") " function returns the numeric
      port used for the database connection " (item parameter "Connection")
      ".")))
