(section
  (ref "pgada_database_oid_status")
  (title "PGAda.Database.OID_Status")
  (para function_desc "Return OID status of result.")
  (subsection
    (title "synopsis")
    (para-verbatim function_synopsis
"with PGAda.Database;

function OID_Status (Result : in PGAda.Database.Result_T) return String;
"))
  (subsection
    (title "description")
    (para
      "The " (item function "OID_Status") " function returns the
      OID status of the result " (item function_param "Result") ".")))
