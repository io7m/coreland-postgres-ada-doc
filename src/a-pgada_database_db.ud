(section
  (ref "pgada_database_db")
  (title "PGAda.Database.DB")
  (para function_desc "Return database name of a connection.")
  (subsection
    (title "synopsis")
    (para-verbatim function_synopsis
"with PGAda.Database;

function DB (Connection : in PGAda.Database.Connection_T) return String;
"))
  (subsection
    (title "description")
    (para
      "The " (item function "DB") " function returns the name of the
      database associated with " (item function_param "Connection") ".")))
