(section
  (ref "pgada_database_finish")
  (title "PGAda.Database.Finish")
  (para function_desc "Closes the connection to the server.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis
"with PGAda.Database;

procedure Finish (Connection : in out PGAda.Database.Connection_T);
"))
  (subsection
    (title "Description")
    (para
      "The " (item ada_procedure "Finish") " procedure closes the connection
       to the database server and frees any storage used.")))
