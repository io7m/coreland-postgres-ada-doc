(section
  (ref "pgada_database_clear")
  (title "PGAda.Database.Clear")
  (para function_desc "Free result storage.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis
"with PGAda.Database;

procedure Clear (Result : in out PGAda.Database.Result_T);
"))
  (subsection
    (title "Description")
    (para
      "The " (item ada_procedure "Clear") " procedure frees the storage associated
      with " (item parameter "Result") ".")))
